package com.nghiahd.server.repository;

import com.nghiahd.server.domain.StatusProfile;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatusProfileRepository extends JpaRepository<StatusProfile, Integer> {
}
